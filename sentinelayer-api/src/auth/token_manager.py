from typing import Optional


def create_jwt(subject: str, secret: str, algorithm: str) -> str:
    """Placeholder for JWT creation."""
    return ""


def verify_jwt(token: str, secret: str, algorithm: str) -> Optional[dict]:
    """Placeholder for JWT verification."""
    return None
